{
  "buildCommand": "cd \"vite +react stowstack v1\" && npm run build",
  "outputDirectory": "vite +react stowstack v1/dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
